A scraper-tool for collecting all ERC20 token metadata from Etherscan.

I find myself needing this every now and then so figured I'd extract it into its own tool.

```bash
go get github.com/gocolly/colly
go run main.go
```
